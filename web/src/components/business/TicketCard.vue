<script setup>defineProps({ ticket: { type: Object, default: () => ({}) } })</script>
<template><article class="ticket-card"><span>预约凭证</span><h3>{{ ticket.title || '活动预约凭证' }}</h3><p>{{ ticket.subTitle || '名额与规则将在活动详情中展示' }}</p><strong>{{ ticket.actualValue ? `¥${(ticket.actualValue / 100).toFixed(0)}` : '待公布' }}</strong></article></template>
<style scoped>.ticket-card { padding:var(--space-5); border:1px solid var(--color-border); border-left:4px solid var(--color-accent); border-radius:var(--radius-md); background:var(--color-surface); }.ticket-card span { color:var(--color-primary); font-size:.75rem; font-weight:700; }.ticket-card h3 { margin:var(--space-2) 0; }.ticket-card p { margin:0 0 var(--space-3); color:var(--color-text-secondary); font-size:.88rem; }.ticket-card strong { color:var(--color-accent); }</style>
