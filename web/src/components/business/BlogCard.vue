<script setup>defineProps({ blog: { type: Object, default: () => ({}) } })</script>
<template><article class="blog-card"><div class="avatar">{{ blog.name?.slice(0, 1) || '城' }}</div><div><strong>{{ blog.name || 'CityHub 参与者' }}</strong><h3>{{ blog.title || '一段活动体验，等待被分享。' }}</h3><p>{{ blog.content || '活动社区将在后续阶段接入真实动态。' }}</p></div></article></template>
<style scoped>.blog-card { display:flex; gap:var(--space-3); padding:var(--space-5); border:1px solid var(--color-border); border-radius:var(--radius-lg); background:var(--color-surface); }.avatar { display:grid; flex:0 0 auto; width:36px; height:36px; place-items:center; border-radius:50%; background:var(--color-primary-soft); color:var(--color-primary); font-weight:700; }.blog-card h3 { margin:var(--space-2) 0; font-size:1rem; }.blog-card p { margin:0; color:var(--color-text-secondary); font-size:.88rem; }</style>
